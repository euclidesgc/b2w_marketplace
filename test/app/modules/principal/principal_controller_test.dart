import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:b2w_marketplace/app/modules/principal/principal_controller.dart';
import 'package:b2w_marketplace/app/modules/principal/principal_module.dart';

void main() {
  initModule(PrincipalModule());
  // PrincipalController principal;
  //
  setUp(() {
    //     principal = PrincipalModule.to.get<PrincipalController>();
  });

  group('PrincipalController Test', () {
    //   test("First Test", () {
    //     expect(principal, isInstanceOf<PrincipalController>());
    //   });

    //   test("Set Value", () {
    //     expect(principal.value, equals(0));
    //     principal.increment();
    //     expect(principal.value, equals(1));
    //   });
  });
}
