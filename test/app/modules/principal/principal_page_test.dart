// import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
// import 'package:flutter_modular/flutter_modular_test.dart';

// import 'package:b2w_marketplace/app/modules/principal/principal_page.dart';

void main() {
  testWidgets('PrincipalPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PrincipalPage(title: 'Principal')));
    //  final titleFinder = find.text('Principal');
    //  expect(titleFinder, findsOneWidget);
  });
}
