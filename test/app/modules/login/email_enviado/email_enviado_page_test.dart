import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('EmailEnviadoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EmailEnviadoPage(title: 'EmailEnviado')));
    //  final titleFinder = find.text('EmailEnviado');
    //  expect(titleFinder, findsOneWidget);
  });
}
