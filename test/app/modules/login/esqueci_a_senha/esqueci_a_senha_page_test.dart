import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('EsqueciASenhaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EsqueciASenhaPage(title: 'EsqueciASenha')));
    //  final titleFinder = find.text('EsqueciASenha');
    //  expect(titleFinder, findsOneWidget);
  });
}
